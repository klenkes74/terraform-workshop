repository_name       = "tf-workshop-demo"
repository_visibility = "private"

# TF_VAR_github_token needs to be set in your environment with a valid GitHub token (repo + delete_repo scopes)
# admin:org is needed, when creating repositories under an organization
